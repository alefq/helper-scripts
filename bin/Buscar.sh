#!/bin/bash
# Search for a given string in bash history and history archive
# generated by backup-historial.sh
# Ale <afeltes@sodep.com.py>
#
strings $HOME/.bash_history $HOME/sysadmin/historicos-bash/history-global-$UID | grep $1 | sort | uniq

